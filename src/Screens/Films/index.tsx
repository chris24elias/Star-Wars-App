import React from "react";
import { View, Text } from "react-native";

interface Props {
    navigation: any;
}

const Films = ({  }: Props) => {
    return (
        <View>
            <Text>Films</Text>
        </View>
    );
};

export default Films;
