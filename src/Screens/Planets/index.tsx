import React from "react";
import { View, Text } from "react-native";

interface Props {
    navigation: any;
}

const Planets = ({  }: Props) => {
    return (
        <View>
            <Text>Planets</Text>
        </View>
    );
};

export default Planets;
