import React from "react";
import { View, Text } from "react-native";

interface Props {
    navigation: any;
}

const People = ({  }: Props) => {
    return (
        <View>
            <Text>People</Text>
        </View>
    );
};

export default People;
