import React from "react";
import { View, Text } from "react-native";

interface Props {
    navigation: any;
}

const Species = ({  }: Props) => {
    return (
        <View>
            <Text>Species</Text>
        </View>
    );
};

export default Species;
